### 01.4 — The seam(s)

Define the project's stable interface(s) — the contract(s) the rest of the system depends on.

**Acceptance criteria**
- *(to define: the seam contract(s) — e.g. a source/connector interface and/or the `(to define: a typed data-access package name, or "n/a" — resolve during the `qcode-charter` pass.)`
  read contract. Use `Result<T>` and validate at the boundary. Record the design as an
  ADR.)*
- A trivial fixture implementation proves the interface compiles and is testable.
